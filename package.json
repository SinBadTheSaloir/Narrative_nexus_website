{
  "name": "narrative-nexus",
  "version": "1.0.0",
  "description": "Narrative analysis library viewer",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node backend/server.js",
    "verify": "node -e \"console.log('Working directory:', process.cwd()); const fs = require('fs'); console.log('Files:', fs.readdirSync('.'));\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
