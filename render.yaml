services:
  - type: web
    name: narrative-nexus-v2
    env: node
    buildCommand: "cd backend && npm install"
    startCommand: "cd backend && npm start"
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
